.side
  .side__header
    .side__header__box
      .side__header__box__username
        = current_user.name
      .side__header__box__btns
        = link_to new_group_path do
          = icon('fas','edit', class: "side__header__box__btn")
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog', class: "side__header__box__btn")
  .side__groups
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません


    -# .side__groups__name
    -#   taigas
    -# .side__groups__message
    -#   まだメッセージがありません